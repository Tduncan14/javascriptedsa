function formatPhoneNumber(numbers) {

    const areaCode = numbers.slice(0, 3).join('');
    const preFix = numbers.slice(3, 6).join('');
    const lineNumber = numbers.sliveO



}

module.exports = formatPhoneNumber;
